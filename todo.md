# leere Map -> "Spaceship lost forever" // keine Punkte keine Schiffe
# Map ohen X -> "Spaceship lost forever" // map nicht leer aber kein Schiff
# Map mit X -> Position zurückgeben // gibt die Position vom Schiff zurück
# y Achse von unten beachten // beginnt beim string bei 0/0
# Beispiel aus Angabe testen // muss wert von den schiff zurückgeben wie zb: 7,2